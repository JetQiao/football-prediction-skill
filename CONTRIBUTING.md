# 贡献指南

感谢参与。提交前请确保：

1. 新 Provider 实现统一领域模型，并附离线契约 fixture；网络请求不得进入单元测试。
2. 模型变更附基线对比，禁止使用比赛开赛后的数据或未来赔率。
3. 新情报字段保留来源、发布时间、可信度和影响边界。
4. HTML 保持单文件、离线、无 CDN，并在桌面和移动端检查布局。
5. 运行 `python -m unittest discover -s tests -p 'test_*.py'`。

请勿提交 API Key、用户报告、第三方完整数据集或不具备再分发许可的内容。
